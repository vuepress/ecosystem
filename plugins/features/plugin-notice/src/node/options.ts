import type { NoticeOptions } from '../shared/index.js'

/**
 * Notice plugin options
 *
 * 通知插件选项
 */
export interface NoticePluginOptions {
  /**
   * Notice configuration
   *
   * 通知配置
   */
  config: NoticeOptions[]
}
