{
  "name": "@vuepress/ecosystem",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --stream build && pnpm build:style",
    "build:style": "pnpm --parallel --stream style",
    "bundle": "cross-env NODE_ENV=production pnpm -r --stream build && pnpm build:style",
    "clean": "pnpm --parallel --stream clean",
    "format": "oxfmt",
    "lint": "oxlint --fix --report-unused-disable-directives --deny-warnings && oxfmt && stylelint **/*.{css,html,scss,vue} --fix",
    "lint:check": "oxlint --report-unused-disable-directives --deny-warnings && oxfmt --check && stylelint **/*.{css,html,scss,vue}",
    "pkg:check-update": "ncu",
    "pkg:update": "pnpm up -r",
    "prepare": "husky",
    "release": "pnpm release:check && pnpm release:version && pnpm release:publish",
    "release:check": "pnpm lint && pnpm clean && pnpm bundle && pnpm test",
    "release:publish": "lerna publish --dist-tag next from-package --yes && unrun scripts/syncNpmmirror.ts",
    "release:version": "lerna version",
    "test": "pnpm test:unit && pnpm test:e2e",
    "test:cov": "cross-env TZ=Etc/UTC vitest run --coverage ",
    "test:e2e": "pnpm --filter ecosystem-e2e e2e:dev && pnpm --filter ecosystem-e2e e2e:build",
    "test:unit": "cross-env TZ=Etc/UTC vitest run",
    "type:check": "tsc --noEmit"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@lerna-lite/cli": "^4.11.5",
    "@lerna-lite/publish": "^4.11.5",
    "@tsdown/css": "^0.21.7",
    "@types/node": "^20.19.37",
    "@types/webpack-env": "^1.18.8",
    "@vitest/coverage-istanbul": "4.1.2",
    "cpx2": "^8.0.0",
    "cross-env": "^10.1.0",
    "http-server": "^14.1.1",
    "husky": "^9.1.7",
    "lightningcss": "^1.32.0",
    "nano-staged": "^0.9.0",
    "npm-check-updates": "^19.6.6",
    "oxc-config-hope": "^0.1.3",
    "oxfmt": "0.42.0",
    "oxlint": "1.57.0",
    "oxlint-tsgolint": "0.18.1",
    "postcss-html": "^1.8.1",
    "publint": "^0.3.18",
    "rimraf": "^6.1.3",
    "sass-embedded": "catalog:",
    "stylelint": "^17.6.0",
    "stylelint-config-hope": "^11.0.0",
    "stylelint-config-html": "^1.1.0",
    "tsconfig-vuepress": "^7.0.0",
    "tsdown": "^0.21.7",
    "typescript": "^6.0.2",
    "unrun": "^0.2.34",
    "vite": "8.0.3",
    "vitest": "4.1.2",
    "vuepress": "catalog:"
  },
  "nano-staged": {
    "**/*": "oxfmt --no-error-on-unmatched-pattern",
    "*.{cjs,js,ts,vue}": "oxlint --fix --deny-warnings --report-unused-disable-directives",
    "*.{css,scss,html,vue}": "stylelint --fix"
  },
  "engines": {
    "node": "^20.19.0 || >= 22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
